# User requirements

Only genuine human dependencies belong here. Codex performs accessible terminal, OCR, container, test, browser, benchmark, Git and documentation work.

## Current action

No terminal, token, account setup or new hosting action is required. Once Codex records final readiness, submit these two links through the Treasury assessment's requested channel:

- Application: https://labelguard-ai-4g5s.onrender.com
- Source: https://github.com/evrimakgul/labelguard-ai

Include the README's warm/cold-start and regulatory-scope caveats; do not promise full regulatory approval or an always-on service.

## Confirmed setup and authority

On 2026-09-08 the user confirmed Render Free ($0/month), no billing information/payment method, prepaid credits or paid subscription, and deployed source 919e241. The supplied existing-service screenshot confirms a live deployment of that commit and the free-instance warning. An earlier New Web Service screenshot had a paid selection; it did not describe the already deployed service. Do not create a duplicate service.

Source publication and CI inspection are authorized. The current request authorizes verification of the existing public deployment and correcting/publishing repository documentation. Do not upgrade plans, add billing information, create resources, change Render settings, or submit the assessment on the user's behalf.

Auto-Deploy is unconfirmed. This is informational, not a submission blocker. Do not enable it merely for this task. A documentation push might trigger a rebuild if it is already enabled; verify the public application remains accessible afterward.

## Credentials and future maintenance

No app secret or OCR credential is required. GitHub authentication stays in the OS keyring; never paste tokens into chat, .env or Git. If an actual host authorization challenge arises, Codex first tries its available access/approval mechanism and then requests only the necessary human action.

Keep the existing Free service available for evaluation and do not delete it. It may sleep naturally; there is no keep-alive workaround. Future account/dashboard actions remain the user's decision, not assumed ongoing permission.
